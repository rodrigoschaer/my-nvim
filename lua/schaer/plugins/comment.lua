-- NOTE: plugins can be added using a lua table {}
-- NOTE: opts = {} forces plugin to load, like require().setup({})
-- Comment code with commands like "gc"
return { "numToStr/Comment.nvim", opts = {} }
